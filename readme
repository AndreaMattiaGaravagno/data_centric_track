#create a new environment
python -m venv /path/to/new/virtual/environment

#activate the environment
source /path/to/new/virtual/environment/bin/activate

#install requirements
python -m pip install -r requirements.txt

#carefully read "data_centric_track.py"

#in "data_centric_track.py" modify the value of the variable "data_dir",
#writing the path to the location in which you would like to save the dataset

#execute training script
python data_centric_track.py

#the first execution will require hours, since it has to download the entire dataset (239.25 GiB)
#it will train the fixed architecture on the original dataset to get you started
#then you can modify the script as you like, and propose your own modifications to the dataset!
